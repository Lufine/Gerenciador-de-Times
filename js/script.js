function navigateTo(page) {
    if (page === 'TeamBalancer') {
        window.location.href = './html/TeamBalancer.html';
    } else if (page === 'game') {
        window.location.href = './html/game.html';
    }
}